import { BasicHighlightRenderer2Directive } from './basic-highlight-renderer2.directive';

describe('BasicHighlightRenderer2Directive', () => {
  it('should create an instance', () => {
    const directive = new BasicHighlightRenderer2Directive();
    expect(directive).toBeTruthy();
  });
});
